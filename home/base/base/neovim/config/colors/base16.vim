" Minimal shim colorscheme for LazyVim
" The actual highlights are applied by base16-colorscheme (data.matugen).
if exists("g:colors_name") && g:colors_name ==# "base16"
  finish
endif
let g:colors_name = "base16"
